import { describe, it, expect } from 'vitest';
import { singleNumber } from './solution';

describe('Single Number', () => {
  it('is defined', () => {
    expect(singleNumber).toBeTypeOf('function');
  });
});
