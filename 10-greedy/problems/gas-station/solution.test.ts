import { describe, it, expect } from 'vitest';
import { canCompleteCircuit } from './solution';

describe('Gas Station', () => {
  it('is defined', () => {
    expect(canCompleteCircuit).toBeTypeOf('function');
  });
});
