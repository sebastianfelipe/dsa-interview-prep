{
  "name": "dsa-interview-prep",
  "private": true,
  "license": "MIT",
  "description": "TypeScript DSA interview prep \u2014 patterns, templates, study UI",
  "workspaces": [
    "api",
    "ui"
  ],
  "scripts": {
    "dev": "concurrently -n api,ui -c teal,cyan \"yarn workspace api dev\" \"yarn workspace ui dev\"",
    "build": "yarn workspace api build && yarn workspace ui build",
    "test:solutions": "vitest run --config vitest.solutions.config.ts",
    "migrate:problems": "tsx scripts/migrate-problems.ts",
    "check": "tsc --noEmit -p api && tsc --noEmit -p ui"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "concurrently": "^9.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  },
  "packageManager": "yarn@1.22.22"
}
