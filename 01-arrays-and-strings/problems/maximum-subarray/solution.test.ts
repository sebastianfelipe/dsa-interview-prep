import { describe, it, expect } from 'vitest';
import { maxSubArray } from './solution';

describe('Maximum Subarray', () => {
  it('is defined', () => {
    expect(maxSubArray).toBeTypeOf('function');
  });
});
