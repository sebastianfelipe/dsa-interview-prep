import { describe, it, expect } from 'vitest';
import { uniquePaths } from './solution';

describe('Unique Paths', () => {
  it('is defined', () => {
    expect(uniquePaths).toBeTypeOf('function');
  });
});
