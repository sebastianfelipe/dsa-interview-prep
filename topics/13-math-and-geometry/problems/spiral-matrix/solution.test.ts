import { describe, it, expect } from 'vitest';
import { spiralOrder } from './solution';

describe('Spiral Matrix', () => {
  it('is defined', () => {
    expect(spiralOrder).toBeTypeOf('function');
  });
});
