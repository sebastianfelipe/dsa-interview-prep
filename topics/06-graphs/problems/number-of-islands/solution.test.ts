import { describe, it, expect } from 'vitest';
import { numIslands } from './solution';

describe('Number of Islands', () => {
  it('is defined', () => {
    expect(numIslands).toBeTypeOf('function');
  });
});
