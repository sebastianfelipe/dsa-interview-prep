import { describe, it, expect } from 'vitest';
import { maxDepth } from './solution';

describe('Maximum Depth of Binary Tree', () => {
  it('is defined', () => {
    expect(maxDepth).toBeTypeOf('function');
  });
});
