import { describe, it, expect } from 'vitest';
import { topKFrequent } from './solution';

describe('Top K Frequent Elements', () => {
  it('is defined', () => {
    expect(topKFrequent).toBeTypeOf('function');
  });
});
