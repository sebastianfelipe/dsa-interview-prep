import { describe, it, expect } from 'vitest';
import { subarraySum } from './solution';

describe('Subarray Sum Equals K', () => {
  it('is defined', () => {
    expect(subarraySum).toBeTypeOf('function');
  });
});
