import { describe, it, expect } from 'vitest';
import { minMeetingRooms } from './solution';

describe('Meeting Rooms II', () => {
  it('is defined', () => {
    expect(minMeetingRooms).toBeTypeOf('function');
  });
});
